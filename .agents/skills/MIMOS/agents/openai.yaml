interface:
  display_name: "MIMOS"
  short_description: "Repo-specific patterns and workflows for MIMOS"
  default_prompt: "Use the MIMOS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true